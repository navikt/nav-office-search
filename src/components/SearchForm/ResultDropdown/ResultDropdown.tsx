import React from 'react';
import { SearchHitProps } from '../../../types/searchHitProps';

type Props = {
    hits: SearchHitProps[];
};

export const ResultDropdown = ({ hits }: Props) => {
    return null;
};
