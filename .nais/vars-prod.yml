ingresses:
  - https://www.nav.no/sok-nav-kontor
accessPolicy:
  outbound:
    external:
      - host: nav.no
      - host: nav-office-search-api.prod-fss-pub.nais.io
    rules:
      - application: nav-office-search-api
        cluster: prod-gcp
        namespace: personbruker